# Build a connection with Eureka "Node.js + Eureka" using framework eureka-js-client

## Build this Project
- Run `npm install` for install node modules.
- Run `npm run dev` for a dev server at port 3000.